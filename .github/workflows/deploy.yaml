name: CI-CD to ArgoCD

on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      # 2️⃣ Get short SHA and short Run ID
      - name: Get short SHA and short Run ID
        id: vars
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "run_id_short=${GITHUB_RUN_ID:0:4}" >> $GITHUB_OUTPUT
          echo "Image tag will be: main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }}"

      # 3️⃣ Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4️⃣ Login to DockerHub
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5️⃣ Build and push Docker image
      - name: Build and Push Docker Image
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          push: true
          platforms: linux/amd64
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/testproj:main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }}
            ${{ secrets.DOCKER_USERNAME }}/testproj:latest

      # 6️⃣ Checkout manifest repository
      - name: Checkout manifest repository
        uses: actions/checkout@v4
        with:
          repository: argahv/k3-manifests
          path: manifests-repo
          token: ${{ secrets.MANIFEST_REPO_TOKEN }}
          persist-credentials: true

      # 7️⃣ Update Rollout manifest with new image tag
      - name: Update Rollout manifest with new image tag
        run: |
          NEW_TAG="main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }}"
          MANIFEST_FILE="manifests-repo/K8s-Manifest/testproj/Argo-Rollout/testproj.yaml"

          echo "🔍 Current image in Rollout manifest:"
          grep "image:" "$MANIFEST_FILE" || echo "ERROR: No image line found!"
          echo ""
          echo "📝 Updating to image tag: $NEW_TAG"

          # Replace any image line (handles PLACEHOLDER, SHA, or any tag)
          sed -i "s|image:.*|image: argahv/testproj:${NEW_TAG}|g" "$MANIFEST_FILE"

          echo ""
          echo "✅ Updated Rollout manifest:"
          grep "image:" "$MANIFEST_FILE"

          # Verify the update worked
          if ! grep -q "image: argahv/testproj:${NEW_TAG}" "$MANIFEST_FILE"; then
            echo "❌ ERROR: Failed to update image tag!"
            exit 1
          fi

      # 8️⃣ Commit and push to manifest repository
      - name: Commit updated manifest
        working-directory: manifests-repo
        env:
          GITHUB_TOKEN: ${{ secrets.MANIFEST_REPO_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # Show current branch and status
          echo "Current branch: $(git branch --show-current)"
          echo "Git status before:"
          git status

          git add K8s-Manifest/testproj/Argo-Rollout/testproj.yaml

          # Show what changed
          echo "Changes staged:"
          git diff --cached K8s-Manifest/testproj/Argo-Rollout/testproj.yaml || echo "No changes detected"

          if git diff --cached --quiet; then
            echo "⚠️  No changes to commit - image tag may already be main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }}"
            echo "Current image in Rollout manifest:"
            grep "image:" K8s-Manifest/testproj/Argo-Rollout/testproj.yaml
          else
            echo "💾 Committing changes..."
            git commit -m "Deploy testproj image main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }} [skip ci]"
            COMMIT_SHA=$(git rev-parse HEAD)
            echo "🚀 Pushing to origin main..."
            git push origin main
            
            # Verify the push was successful
            echo ""
            echo "✅ Successfully pushed updated Rollout manifest"
            echo "📌 Manifest Commit SHA: $COMMIT_SHA"
            echo "📌 Docker Image Tag: main-${{ steps.vars.outputs.sha_short }}-${{ steps.vars.outputs.run_id_short }}"
            echo ""
            echo "Verifying remote commit..."
            git fetch origin
            REMOTE_SHA=$(git rev-parse origin/main)
            if [ "$COMMIT_SHA" = "$REMOTE_SHA" ]; then
              echo "✅ Remote commit verified: $REMOTE_SHA"
            else
              echo "⚠️  Warning: Local and remote SHAs don't match"
            fi
            echo ""
            echo "🔔 ArgoCD should detect this change and auto-sync within 3 minutes"
            echo "📋 Check ArgoCD UI to see if it picks up commit: $COMMIT_SHA"
          fi
